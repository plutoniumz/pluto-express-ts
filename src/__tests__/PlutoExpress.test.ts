import { app } from '../utils'
const PlutoExpress = require('../PlutoExpress')

describe('PlutoExpress', () => {
    describe('startApplication', () => {
        describe('should call app', () => {
            it('with correct default port', () => {
                PlutoExpress()

                expect('')
            })
        })
    })
})
